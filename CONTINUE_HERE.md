# RadioPartsDB — состояние на 2026-06-03

## Что уже реализовано (Desktop)
- Экспорт в CSV/Excel (учитывает фильтры)
- Резервное копирование через Яндекс.Диск (БД + images + datasheets) с отложенным восстановлением
- Пакетное редактирование (все поля, включая номинал)
- Окно просмотра детали с поддержкой до 3 изображений (URL и локальные)
- Умная сборка названия, размеры конденсаторов, выпадающие списки
- Рефакторинг `main_window.py` (разделение на actions/handlers)
- Исправлено отображение веб-картинок (URL) в правой панели и окне просмотра
- Локальные изображения копируются в `data/images`, сжимаются в WebP

## Неисправленные проблемы (важно)
1. **Веб-клиент (SPA) не синхронизирован** – отсутствуют:
   - Поддержка трёх изображений (только одно поле)
   - Единицы измерения (value_unit) в таблице и форме редактирования
   - Размеры конденсаторов (diameter_mm, height_mm и др.)
   - Кнопка «Собрать название»
   - Сохранение настроек (фильтр, поиск) в localStorage
2. **Остальные баги, которые вы заметили** – жду их список.

## Текущая ветка
`refactoring-main-window` (или `main`, уточните)

## Что требуется от нового чата
- Доработать веб-клиент (синхронизировать с Desktop).
- Исправить перечисленные вами баги (как только вы их перечислите).
- При необходимости помочь с коммитом и слиянием веток.

## Прилагаемые файлы (опционально)
Если удобно, приложите в новом чате следующие файлы (текущие версии):
- `frontend/index.html`
- `frontend/js/modals.js`
- `frontend/js/parts.js`
- `frontend/js/settings.js`
- `frontend/js/utils.js`
- `ui/dialogs/part_dialog.py`
- `ui/dialogs/part_viewer.py`
- `ui/widgets/info_panel.py`
- `ui/main_window.py`
- `config.py`

Я отвечу в новом чате и продолжу работу.