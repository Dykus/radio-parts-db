# Журнал изменений (Changelog)

Все значимые изменения в проекте `RadioPartsDB` будут документироваться в этом файле.  
Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/),  
версионирование — на [Semantic Versioning](https://semver.org/lang/ru/).

---

## [0.21.0] — 2026-05-27

### ✨ Добавлено (Desktop)

#### Пакетное редактирование
- Возможность выделить несколько деталей в таблице (Ctrl+клик, Shift+клик) и изменить общие поля массово.
- В диалоге пакетного редактирования можно изменить:
  - Категорию (выбор из дерева)
  - Место хранения
  - Статус
  - Тип детали (с добавлением нового в словарь)
  - Производителя (с добавлением нового в словарь)
  - Цену (установить, увеличить, уменьшить)
  - Количество (установить, добавить, отнять)

#### Улучшенное сохранение состояния окна
- Теперь запоминается не только размер и позиция, но и **развёрнутое состояние** (максимизировано/обычное).
- Окно восстанавливается именно в том виде, в котором было закрыто.

#### Яркое выделение строк в таблице
- Выделенные строки подсвечиваются контрастным **синим фоном** с **белым текстом**.
- Выделение хорошо видно даже на цветных строках (зелёных, жёлтых, красных).

#### Габариты конденсаторов
- В диалог редактирования добавлены поля для размеров: **диаметр (мм)**, **высота (мм)**, **шаг выводов (мм)**, **толщина выводов (мм)**.
- Поля появляются только при выборе категории, содержащей слово «Конденсатор».
- В таблице в колонке «Корпус» автоматически отображаются размеры в формате `⌀10×17мм`, `шаг 5мм`, `вывод 0.5мм`.

#### Словари для типов деталей и производителей
- Поля «Тип детали» и «Производитель» теперь представляют собой **выпадающие списки** с возможностью добавления новых значений.
- Новые значения автоматически сохраняются в базу данных и появляются в списке при следующем вводе.
- При первом запуске словари заполняются начальными значениями.

#### Улучшенная сборка названия
- Корректно учитывается **единица измерения**, выбранная из выпадающего списка (например, `10 Ом`).
- Для резисторов из категории извлекается **мощность** (например, `0.5 W` → `0.5 Вт`).
- Для конденсаторов из категории извлекается **напряжение** (например, `10V` → `10В`).
- Порядок сборки: `[номинал с единицей] [напряжение/мощность] [корпус]`.

#### Индикатор фото в таблице
- В колонке «Корпус» перед текстом отображается 📷, если у детали указан путь к изображению.

#### Кнопка «Сегодня» для даты ревизии
- В диалог редактирования добавлена кнопка, устанавливающая текущую дату.

#### Полная русификация контекстного меню
- Во всех текстовых полях, полях ввода комбобоксов, полях заметок и числовых полях контекстное меню полностью переведено на русский язык (Отменить, Повторить, Вырезать, Копировать, Вставить, Удалить, Выделить всё).

### 🐛 Исправлено
- Убрано `.0` у целых чисел в нормализованном номинале (было `10.0 Ом`, стало `10 Ом`).
- При выборе детали без фото правая панель очищается (не остаётся предыдущего изображения).
- Исправлена ошибка `TypeError` при работе с `None` в полях размеров.
- Исправлено отсутствие сигнала `double_clicked` в `PartsTableWidget` (двойной клик по строке теперь открывает редактирование).
- Окно программы теперь корректно запоминает своё развёрнутое состояние, размер и позицию.

### 🔧 Изменено
- База данных обновлена до схемы **версии 4** (добавлены поля диаметра, высоты, шага выводов, толщины выводов).
- Метод `get_all_parts_filtered` теперь возвращает `image_path` и поля размеров.
- Словари `part_type` и `manufacturer` заполняются начальными значениями при первом запуске.
- Улучшена производительность загрузки таблицы.

---

## [0.20.3-beta] — 2026-05-27

### 🎨 Улучшено (Web)
- **Цветовая схема таблицы** (аналог Desktop):
  - Фон строки зависит от количества: 🟢 зелёный (>10), 🟡 жёлтый (1-9), 🔴 красный (0)
  - Цвет ячейки «Статус»: «Новое/Отличное» — зелёный, «Б/У проверено» — голубой, «Б/У не проверено» — жёлтый, «Плохое» — оранжевый, «Неисправно» — красный.
- Использованы inline-стили для надёжности.

### 🐛 Исправлено (Backend)
- В метод `get_all_parts_filtered` добавлено поле `category_id`, чтобы веб-клиент мог фильтровать детали по категориям.

---

## [0.20.2-beta] — 2026-05-27

### ✨ Добавлено (Desktop)
- **Умное формирование названия детали**:
  - При выборе категории автоматически подставляются соответствующие единицы измерения (для конденсаторов – пФ/нФ/мкФ/Ф, для резисторов – Ом/кОм/МОм и т.д.).
  - Кнопка «Собрать название» теперь формирует имя по шаблону: `[Номинал][Единица] [Напряжение] [Корпус]`.
  - Напряжение извлекается из последнего сегмента категории (например, `25V` из `Конденсатор / Электролитический / 25V`).
- **Нормализация ввода номинала**: поле «Номинал / значение» теперь принимает только число, единица выбирается из выпадающего списка.

### 🔧 Изменено
- Парсер номинала упрощён: теперь извлекается только число, единица – из списка.
- Динамическое обновление единиц измерения при смене категории.

---

## [0.20.1-beta] — 2026-05-27

### ⌨️ Добавлено (Desktop)
- **Горячие клавиши** для основных действий:
  - `Ctrl+N` — открыть диалог добавления новой детали.
  - `Ctrl+E` — редактировать выбранную деталь.
  - `Del` — удалить выбранную деталь (с подтверждением).
  - `Ctrl+F` — установить курсор в поле поиска.
  - `F5` — полностью обновить интерфейс (таблицу, категории, навигатор).

### 🔧 Изменено
- Файл `ui/main_window.py` дополнен обработчиками горячих клавиш и методом `_focus_search()`.

---

## [0.20.0-beta] — 2026-05-26

### 🚀 Добавлено (SPA-эксперимент)
- **Одностраничное приложение (SPA)**: Веб-интерфейс полностью переписан на чистом JavaScript без перезагрузок страницы.
- **Динамическое дерево категорий**: Категории загружаются с бэкенда, при клике таблица мгновенно фильтруется по выбранной категории.
- **Фильтры без перезагрузки**: Кнопки «Все», «В наличии», «Мало», «Нет» работают через AJAX, таблица обновляется на лету.
- **Живой поиск**: Фильтрация таблицы по всем полям (наименование, тип, корпус, место, статус) по мере ввода текста.
- **Визуальное выделение активной категории**: Выбранная категория подсвечивается синим фоном.
- **Обновление статистики**: Статистика (количество деталей, общая стоимость) пересчитывается автоматически при изменении данных.

### 🐛 Исправлено (Web Client)
- Устранены проблемы с загрузкой категорий и деталей из-за неполных данных в API (добавлена обработка отсутствующих полей).

### 🔧 Изменено
- Веб-клиент (`frontend/index.html`) теперь полностью самодостаточен и не требует дополнительных сборщиков.

---

## [0.19.1] — 2026-05-26

### 🐛 Исправлено
- **Валидация Pydantic V2**: Устранена ошибка `ResponseValidationError` при отдаче данных из API. Реализован патч словарей БД для гарантированного наличия всех опциональных полей.
- **Синтаксис моделей**: Переход на `ConfigDict` вместо устаревшего `class Config`.
- **Запуск проекта**: `run.bat` теперь автоматически устанавливает зависимости и запускает все три компонента (Backend, Desktop, Web) без ручного вмешательства.

### ✨ Добавлено
- Полный файл `requirements.txt` с зависимостями для Backend (FastAPI, Uvicorn, Pydantic).
- Автоматическая проверка и создание виртуального окружения при первом запуске.

### 🔧 Изменено
- Версия обновлена до `0.19.1`.

---

## [0.19.0] — 2026-05-24

### ✨ Добавлено
- **Backend API**: Сервер на FastAPI с полным CRUD, Swagger UI (`/docs`) и CORS.
- **Web Client**: Простой HTML/JS интерфейс, загружающий данные из API.
- **Smart Launcher**: `run.bat` теперь запускает Backend, Desktop и Web одновременно.
- **Безопасный SQLite**: Включён режим `WAL` и `check_same_thread=False` для корректной работы с несколькими подключениями.

### 🐛 Исправлено
- **Pydantic V2 Fix**: Патч словарей БД перед сериализацией для предотвращения `ResponseValidationError`.
- Ошибка `PydanticDeprecatedSince20` (заменено `class Config` на `ConfigDict`).
- Мгновенное закрытие `run.bat` из-за проблем с `activate.bat` и кодировкой.
- Отсутствие полей `category_id`, `image_path` в ответах API вызывало краш валидации.

### 🔧 Изменено
- Ядро `core/database.py` полностью изолировано и переиспользуется в Desktop и API.
- Архитектура переведена в режим `Backend + Frontend` с сохранением совместимости.
- Версия обновлена до `0.19.0` в `config.py`.

---

## [0.18.0] — 2026-05-24

### Исправлено
- **Логика раскрытия деревьев:** Полностью переписан механизм управления глубиной категорий и навигатора. Заменён ненадёжный метод Qt `expandToDepth()` на рекурсивную функцию `_apply_depth_recursive()`.
- **Конфликт интерфейсов:** Устранено дублирование элементов управления в правой панели.
- **Исчезновение данных:** Исправлена ошибка, из-за которой центральная таблица могла очищаться при изменении настроек глубины.

### Добавлено
- **Жирный шрифт для категорий:** Категории, содержащие детали (>0), отображаются жирным шрифтом.
- **Перетаскивание колонок:** Возможность менять порядок столбцов таблицы мышкой.
- **Сохранение порядка колонок:** Конфигурация столбцов сохраняется и восстанавливается.

---

## [0.17.0] — 2026-05-23

### ✨ Добавлено
- **Интерактивные настройки:** Настройки глубины деревьев применяются мгновенно, без перезапуска.
- **Inline-управление:** Выпадающие списки глубины прямо над деревьями в главном окне.
- **Кнопка быстрой глубины:** Циклическое переключение уровней (0→1→2→3→Всё).
- **Управление таблицей:** Перетаскивание колонок с сохранением порядка между сессиями.

---

_Полный список изменений за всё время см. в репозитории GitHub._

---

## Ссылки

- [Репозиторий проекта](https://github.com/Dykus/radio-parts-db)
- [Формат Keep a Changelog](https://keepachangelog.com/ru/1.0.0/)